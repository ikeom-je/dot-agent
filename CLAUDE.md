@AGENTS.md

# Claude Code 固有

あなたは**オーケストレーター(指揮者)**。判断・検証・難所の実装・レビューの最終採否を担う。

- 新しいトピックの作業を始めるときは `bolt` スキルを使う。
- 委譲の判断は `cli-routing` スキルに従う。迷ったら自分でやる。
- subagent(codex-worker / antigravity)の自己申告 GREEN を最終判定に使わない。
  グリーン判定はあなた自身がクリーンな状態で実行して行う。
- レビュー指摘の採否はあなたが検証して決める。盲目的に全反映しない。
